<div class='slider-wrap'>
	{{#if gallery}}
		{{#each gallery.images}}
			<div class='slider-image {{#is @index position}}current{{/is}}' style='background-image: url("/img/uploads/{{.}}");'></div>
		{{/each}}
    {{/if}}
	<div class='slider-arrow left'></div>
	<div class='slider-arrow right'></div>
</div>
{{log 'bla'}}