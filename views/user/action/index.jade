extends ../layout

block append header
	link(href="/css/user/action/action.css" rel="stylesheet")

block content
	.module.server.action
		include content

block append scripts
	script#counter_mustache(type="text/mustache")
		- var server = false
		mixin counter(server)