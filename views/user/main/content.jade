.mainModuleWrap
	img.apsBg(src="/img/user/main/topper_background.jpg")
	.content.bannerWrap(style = (user ? 'display: none' : ''))
		.whats_up
		
		.topper
			.top_label
				| НОВЫЙ
				br
				| ЙОГУРТ со злаками
			
			.label.novito_nova
				| ПОЛЬЗА МОЛОКА
				br
				| И ЗЛАКОВ В ОДНОЙ
				br
				| БУТЫЛОЧКЕ
			
			button.banner_button Это вкусно?
		
		include ../helpers/auth_box

	include ../registered/content
	
	- if (!user)
		.content.itemsWrap.commonFeed
			.items_container
				- each a, i in articles.documents
					mixin article(a, i)
		if (articles.nextAnchorId)
			.loadMore
				img(src="/img/user/main/icon_book.gif")
